---
interface Props {
  ref: string
  title: string
}
const { ref, title } = Astro.props
---

<a
  href={`#${ref}`}
  class="px-4 py-2 text-sm font-medium text-custom-lightGray rounded-full transition-all duration-300 hover:text-white hover:bg-glass-3"
>{title}</a>
